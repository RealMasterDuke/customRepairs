---
applications:
- name: customRepairs
  memory: 256M
  instances: 1
  host: duke-customRepairs
  domain: de.a9sapp.eu
  env:
      RAILS_ENV: production
      RACK_ENV: production
  services:
      - postgresql-customRepairs
      - swift-customRepairs
